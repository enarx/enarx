[package]
name = "rust_syscall_tests"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
goblin = { version = "0.5", default-features = false, features = [ "elf64" ] }
rcrt1 = "1.0.0"
sallyport = "0.4.0"
