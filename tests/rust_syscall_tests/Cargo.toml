[package]
name = "rust_syscall_tests"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
goblin = { version = "0.5", features = ["elf64"], default-features = false }
rcrt1 = { version = "2.2.0", default-features = false }
sallyport = { version = "0.4.0", default-features = false }
