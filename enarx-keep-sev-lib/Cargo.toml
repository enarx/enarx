[package]
name = "enarx-keep-sev-lib"
version = "0.1.0"
authors = ["Harald Hoyer <harald@redhat.com>"]
license = "Apache-2.0"
edition = "2018"

categories = [
    "no-std",
]

keywords = [
    "no_std",
]

[dependencies]
serde = { version = "1.0", default-features = false, features = ["derive"] }
